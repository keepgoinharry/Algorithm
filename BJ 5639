#include <bits/stdc++.h>
using namespace std;
vector<int> nums;

void postorder(int start, int end)
{
  if(start>=end)return;
  int root = nums[start];
  int idx = start+1;

  while(idx<end&&nums[idx]<root)idx++;

  postorder(start+1, idx);
  postorder(idx,end);

  cout<<root<<"\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int x;
    while (cin >> x) {
        nums.push_back(x);
    }

    postorder(0, nums.size());//start,end
}
