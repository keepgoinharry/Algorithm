#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using pi=pair<int,int>;

int d(int num)
{
  int res = num;
  string s = to_string(num);
  for(int i=0; i<s.size(); i++)
    {
      res+=s[i]-'0';
    }
  return res;
}
int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  map<int,int> m;
  for(int i=1; i<=10000; i++)m[i] = 0;

  for(int i=1; i<=10000; i++)
    {
      int next = d(i);
      if(next<=10000)
        m[next]++;
      
    }
  for(int i=1; i<=10000; i++)
    {
      if(m[i]==0)cout<<i<<"\n";
    }
}
