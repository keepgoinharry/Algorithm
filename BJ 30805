#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pi = pair<int,int>;
ll N,M;

bool cmp(pi a,pi b)
{
  if(a.first == b.first) return a.second<b.second;
  return a.first>b.first;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  cin>>N;
  vector<pi> v1,v2;
  for(int i=0; i<N; i++)
    {
      int tmp;
      cin>>tmp;
      v1.push_back({tmp,i});
    }
  cin>>M;
  for(int i=0; i<M; i++)
    {
      int tmp;
      cin>>tmp;
      v2.push_back({tmp,i});
    }

  sort(v1.begin(),v1.end(),cmp);
  sort(v2.begin(),v2.end(),cmp);

  int idxa = 0;
  int idxb = 0;
  int lasta = -1;
  int lastb = -1;

  vector<int> answer;
  while(idxa<N&&idxb<M)
    {
      if(v1[idxa].first>v2[idxb].first)idxa++;
      else if(v1[idxa].first==v2[idxb].first)
      {
        if(lasta>v1[idxa].second)idxa++;
        else if(lastb>v2[idxb].second)idxb++;
        else
        {
          answer.push_back(v1[idxa].first);
          lasta = v1[idxa].second;
          lastb = v2[idxb].second;
          idxa++;idxb++;
        }
        
      }
      else idxb++;
    }

  cout<<answer.size()<<endl;
  for(auto i:answer) cout<<i<<" ";
  
}
