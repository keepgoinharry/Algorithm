#include <bits/stdc++.h>
using namespace std;
using ll = long long;

bool is_number(const std::string& str) {
    return !str.empty() && std::all_of(str.begin(), str.end(), ::isdigit);
}

int main() {
  ios::sync_with_stdio(false); // C 표준 입출력과 동기화 끔
  cin.tie(nullptr);
  int N,M;
  cin>>N>>M;
  map<string,bool> m1;
  map<string,bool> m2;
  set<string> v;
  for(int i=0; i<N; i++)
    {
      string tmp;
      cin>>tmp;
      m1[tmp]=true;
      v.insert(tmp);
    }
  for(int j=0;j<M; j++)
    {
      string tmp;
      cin>>tmp;
      m2[tmp]=true;
      v.insert(tmp);
    }
  vector<string> answer;
  for(auto i:v)
    {
      if(m1[i]&&m2[i])answer.push_back(i);
    }
  cout<<answer.size()<<"\n";
  for(auto i:answer)cout<<i<<"\n";
}

